#gets and splits user inputs
userInput = input()
phrase = userInput.split()
acro = ""

#loops through each word in the phrase
for s in phrase:

  #checks if first letter in word is uppercase
  if s[0].isupper():
    #beginning letter is added to the acronym in uppercase
    acro += f"s[0].upper()}."
    
#print acronym
print(acro)
